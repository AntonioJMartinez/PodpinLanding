{
  "name": "podpin-landing",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build:site": "node scripts/build-site.mjs",
    "prepare:hosting": "node scripts/prepare-hosting.mjs",
    "build": "npm run build:site && npm run prepare:hosting && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.37.1",
    "@openai/sites-vite-plugin": "0.2.0",
    "vite": "8.0.13",
    "wrangler": "4.92.0"
  }
}
